name: PR11 Evidence Materialization Repair

on:
  push:
    branches: [governance/live-conversation-runtime]
    paths:
      - 'governance-runtime/platform_candidate_review_v2.py'
      - 'governance-runtime/platform_candidate_review_v7.py'
      - 'governance-runtime/test_platform_candidate_review_v2_materialization.py'
      - 'governance-runtime/test_platform_candidate_review_v7.py'
      - 'governance-runtime/repair-preregistrations/GOV-EVIDENCE-MATERIALIZATION-001.md'
      - 'governance-runtime/repair-preregistrations/GOV-REVIEWER-SERVING-PROVIDER-PIN-001.md'
      - '.github/workflows/governance-candidate-platform-review.yml'
      - '.github/workflows/pr11-evidence-materialization-repair.yml'
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  deterministic-repair:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout governance branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run evidence materialization regression tests
        run: |
          cd governance-runtime
          python -m unittest -v test_platform_candidate_review_v2_materialization.py
          python test_platform_candidate_review_v7.py
      - name: Assert authoritative workflow preserves materialization through v7
        run: |
          grep -q 'platform_candidate_review_v7.py' .github/workflows/governance-candidate-platform-review.yml
          grep -q 'platform_candidate_evidence_v3' governance-runtime/platform_candidate_review_v7.py
          ! grep -q 'python governance-runtime/platform_candidate_review.py ' .github/workflows/governance-candidate-platform-review.yml
