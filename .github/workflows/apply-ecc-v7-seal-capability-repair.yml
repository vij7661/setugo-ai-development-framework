name: Apply ECC V7 Seal Capability Repair

on:
  push:
    branches:
      - experiments/ecc-derived-parallel-v7-seal-capability
    paths:
      - '.github/scripts/apply_ecc_v7_seal_capability_repair.py'
      - '.github/workflows/apply-ecc-v7-seal-capability-repair.yml'

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact V7 branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Record repair input
        run: |
          echo "REPAIR_INPUT_SHA=$(git rev-parse HEAD)"
          git status --short

      - name: Apply deterministic V7 seal-capability repair
        run: python .github/scripts/apply_ecc_v7_seal_capability_repair.py

      - name: Commit exact repair
        shell: bash
        run: |
          set -euo pipefail
          git diff --check
          git status --short
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            experiments/ecc_derived/ecc_candidate_boundary.py \
            experiments/ecc_derived/ecc_governance_trust_manifest.json
          git diff --cached --exit-code && { echo "No repair diff produced"; exit 1; } || true
          git commit -m "Hide ECC V7 positive seal capability"
          git push origin HEAD:experiments/ecc-derived-parallel-v7-seal-capability
