{
  "schema_version": 1,
  "fixture_id": "GROQ-PROVIDER-SMOKE-SUBJECT-001",
  "purpose": "Minimal candidate used only to verify authenticated Groq transport, exact provider/model binding, semantic JSON schema validation, and fail-closed evidence handling.",
  "expected_truth": {
    "provider_must_equal_frozen_request": true,
    "reviewer_identity_is_not_authority_by_itself": true,
    "green_ci_is_not_semantic_authority": true,
    "slice5_authority_effect": "NONE"
  }
}
