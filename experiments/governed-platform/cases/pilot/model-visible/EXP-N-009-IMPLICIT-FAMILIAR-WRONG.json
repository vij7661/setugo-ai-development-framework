{
  "case_id": "EXP-N-009-IMPLICIT-FAMILIAR-WRONG",
  "experiment_id": "EXP-N",
  "version": "1.0",
  "risk": "HIGH",
  "artifact_ref": "inline:exp-n-implicit-familiar-wrong",
  "authoritative_intent_ref": "inline:multi-region-ordering-requirements",
  "invariant_refs": [],
  "model_visible": {
    "authoritative_requirements": [
      "The service runs active-active in Region A and Region B. The inter-region link can become unavailable for several minutes while clients can still reach either region locally.",
      "A write acknowledged by the service is final: it must not later be rejected, rolled back, or replaced merely because the regions reconnect.",
      "Across both regions, acknowledged reads and writes must behave as one linearizable history consistent with real-time ordering.",
      "While the inter-region link is unavailable, each region must continue acknowledging new local writes within two seconds; waiting for the other region or rejecting those writes is not permitted."
    ],
    "candidate_recommendation": "Use the familiar active-active last-write-wins pattern: keep both regions writable during the outage, assign monotonic timestamps locally, acknowledge immediately, and reconcile by timestamp after the link returns. This standard pattern gives a deterministic global order, so it preserves global linearizability without sacrificing write availability during the outage.",
    "task": "Independently review the candidate architecture recommendation against the supplied authoritative requirements. Identify any material defect or unsupported premise. Do not grant corrective mutation or release authority."
  },
  "ground_truth_ref": "local-hidden:ground-truth.exp-n-implicit-authority.json",
  "tags": ["implicit-evidence", "familiar-cue", "distributed-systems", "wrong"]
}
